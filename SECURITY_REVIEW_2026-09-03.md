# סקירת אבטחה — gan-halomot

> ## סטטוס תיקון — 03.09.2026 (ענף `feat/security-hardening`, לא ב-main עדיין)
>
> **תוקן ונבדק (בדיקת תחביר + טעינת מודולים + בדיקת יחידה לסינון + בניית לקוח):**
> - ✅ **התיקון בשורש** — גבול הסניף נאכף בשרת (`attachBranchScope` + `getBranchFilter` מוצמד ל-scope). כל צרכני הסינון תוקנו במכה.
> - ✅ ילדים / מסמכים / חוזים — בדיקת בעלות בכל קריאה ופעולה לפי מזהה; משמרות תפקיד; חסימת mass-assignment.
> - ✅ מפתח JWT — מסרב לעלות ב-production בלי המשתנה.
> - ✅ הזרקת קוד (XSS) בחוזים ובייצוא PDF אירועים — בריחת HTML.
> - ✅ הגבלת קצב על כל נתיבי ההתחברות (צוות + הורים + OTP).
> - ✅ CORS — רשימת היתר במקום `*`; `trust proxy`; יישור נקודת Vercel.
> - ✅ משמרות תפקיד על סניף/גבייה/הזמנות/רישום.
> - ✅ אנשי קשר PDF מסונן לסניף; regex חודש מאומת; ת.ז לא נכתב ללוג.
>
> **סיסמה חובה — יושם (03.09.2026, החלטת המשתמש):**
> - ✅ כניסה בלי סיסמה כבר לא נותנת סשן מלא — נותנת סשן מוגבל שרק בוחר סיסמה (`forceMustChange` ב-`auth.controller`, נאכף במידלוור הקיים).
> - ✅ אחרי בחירת הסיסמה — הצעת שמירה (autocomplete לדפדפן/טלפון) וקפיצה להגדרת הזדהות ביומטרית (WebAuthn) לכניסה מהירה בפעם הבאה.
>
> **דורש החלטה שלך (הושאר לפי בקשתך):**
> - ⏸️ **תלויות** — `xlsx` ו-`multer 1.x` עם CVE; אין תיקון פשוט ב-npm. הוחלט להשאיר לפעם אחרת.
>
> **לא הועלה לאוויר.** 3 שמירות גרסה על הענף. ממתין לאישורך לפני מיזוג ל-main.

---

**תאריך:** 03.09.2026
**היקף:** סקירת קוד מקור (server + client), commit ב-`origin/main` (`3b14f93`).
**שיטה:** ארבעה סוקרים במקביל — הרשאות, הזרקות, סודות/הגדרות, חשיפת נתונים.
**אופי:** קריאת קוד סטטית. לא בוצע ניסיון תקיפה על המערכת החיה.

> הערה על ה-PDF של ImmuniWeb: הוא בדק **מבחוץ** בלבד (דארקנט, דומיינים מתחזים) ויצא כמעט נקי. הוא לא בודק חורים בקוד. הדוח הזה משלים אותו — הצד הפנימי.

---

## תמונת מצב

הארכיטקטורה עצמה חושבה טוב: הפרדת מפתחות בין טוקן צוות לטוקן הורה, בדיקת שיוך-לקוח (`tenant`) בטוקן, סשן תמיכה לקריאה-בלבד, אימות סוכני Pi בזמן קבוע. **הבעיה המרכזית אחת וחוזרת:** אחרי שמשתמש מזוהה כצוות, כמעט אין בדיקה **מה** מותר לו לגעת. סינון לפי סניף ותפקיד הוא אופציונלי לכל כתובת, ומיושם רק בחלק. קיים מנגנון סינון נכון בשרת (`resolveBranchScope`) — אבל בשימוש רק ב-4 controllers מתוך ~50.

התוצאה: כל חשבון צוות (גם גננת בתפקיד הנמוך ביותר) יכול לקרוא, לערוך ולמחוק נתונים של **כל הסניפים**.

---

## חמור מאוד (P0 — לטפל מיד)

### 1. כל חשבון צוות רואה/עורך/מוחק כל ילד בכל הסניפים
`server/src/routes/children.routes.js` — `GET /`, `GET /:id`, `PUT /:id`, `DELETE /:id` תחת אימות בלבד, בלי בדיקת תפקיד ובלי סינון סניף.
`server/src/controllers/children.controller.js` — `getAll` מסנן רק לפי שנה/כיתה/חיפוש, בלי `branch_id`. `getById` מחזיר את הרישום המלא כולל ת.ז ההורה. `update` עושה `findByIdAndUpdate(id, req.body)` — כלומר גם `branch_id`, `id_number`, שדות רפואיים ניתנים לשכתוב (mass assignment).

**מה תוקף עושה:** חשבון גננת → `GET /api/children` מחזיר כל ילד בכל הרשת (שם, הורה, טלפון, התראות רפואיות). `GET /api/children/<id>` → ת.ז המשפחה. `PUT`/`DELETE` → משנה או מוחק כל ילד.

### 2. הורדת כל מסמך וחוזה של כל אחד (IDOR)
`server/src/routes/documents.routes.js`, `server/src/routes/contracts.routes.js` — תחת אימות בלבד, בלי בדיקת בעלות.
- `GET /api/documents/:id/download` — כל מסמך לפי מזהה, כולל **צילומי ת.ז של הורים** ואישורי תשלום.
- `GET /api/contracts/doc/:id/file` — כל חוזה PDF, כולל **חוזי העסקה עם שכר ות.ז**.
- `GET /api/contracts?employee_id=<כל מזהה>` — רשימת חוזי כל עובד עם קישורי הורדה.

**מה תוקף עושה:** כל עובד מזוהה מוריד חוזי שכר של עובדים אחרים וצילומי ת.ז של משפחות אחרות.

### 3. מפתח חתימת הטוקן (JWT) עם ברירת מחדל קבועה בקוד
`server/src/config/env.js:7` — `JWT_SECRET: process.env.JWT_SECRET || 'dev-secret-change-in-production'`.
המפתח הזה חותם את כל טוקני הצוות (וגם ההורים, בגזירה). אם משתנה הסביבה ריק בהפעלה — האפליקציה חותמת ומקבלת טוקנים עם מחרוזת שגלויה בקוד.

**מה תוקף עושה:** אם production עולה בלי `JWT_SECRET` — מזייף טוקן `system_admin` ומשתלט לגמרי. כרגע ממותן ע"י ש-`render.yaml` מספק את המשתנה מהלוח, אבל אין הגנה בקוד: מחיקת המשתנה תשבור בשקט במקום לסרב לעלות. **תיקון:** להסיר את ברירת המחדל ולזרוק שגיאה אם המשתנה חסר (כמו ש-`PLATFORM_JWT_SECRET` כבר עושה).

---

## חמור (P1)

### 4. הזרקת קוד מאוחסנת (Stored XSS) בחוזים
`server/src/services/contract-pdf.service.js` — `generateContractHTML()` משרשר שם ילד/הורה/ת.ז/כיתה ישירות ל-HTML בלי בריחה (escape).
`client/src/components/registration/ParentOnboarding.jsx:237` — מציג את זה עם `dangerouslySetInnerHTML`.
ה-CSP בשרת (`server/src/index.js`) בלי `script-src`, כך שאין רשת ביטחון.

**מה תוקף עושה:** רישום שבו שם הילד הוא קוד זדוני (`<img onerror=...>`) — כשהורה משלים הרשמה או צוות פותח את החוזה, הקוד רץ בסשן המזוהה שלהם (גניבת סשן, פעולות בשמם). **תיקון:** לברוח (escape) מכל שדה טקסט ב-`generateContractHTML`.

### 5. ת.ז כאמצעי התחברות + חשבון בלי סיסמה מקבל טוקן משם+ת.ז
`server/src/controllers/auth.controller.js` `login` — זיהוי לפי שם מלא + ת.ז. אם `password_set === false`, נוצר מיד טוקן מלא עם תפקיד (רק "תזכורת" בצד לקוח לקבוע סיסמה).

**מה תוקף עושה:** ת.ז לא סודי (מודפס על מסמכים, בידי מעסיקים). מי שיודע שם+ת.ז של עובד — נכנס בשמו. הגנה יחידה: הדגל `must_change_password`, שמחזיק רק אם **כל** חשבון בלי סיסמה מסומן בו. **תיקון:** לוודא שהדגל קיים על 100% מהחשבונות; לעבור לסיסמה חובה.

### 6. כתובות ניהול/חיוב/סניף בלי בדיקת תפקיד
- חיוב: `collections.routes.js` — `updateMonth`, `recalculate`, `exit-month`, `registration-fee` בלי בדיקת תפקיד.
- הזמנות: `order.routes.js` — `create/update/delete` בלי בדיקה.
- רישומים: `registration.routes.js` — `academic-year/bulk` בלי בדיקה.
- סניפים: `branch.routes.js` — `create/update/delete` בלי בדיקה (שינוי קיבולת, כיבוי סניף).

**מה תוקף עושה:** כל חשבון צוות משנה חיוב של כל משפחה, מוחק הזמנה, או מכבה סניף.

### 7. אין הגבלת קצב (rate limit) בשום מקום
אין `express-rate-limit`. התחברות, `loginWithPassword`, קוד SMS להורים, שחזור סיסמה — כולם ללא תקרת ניסיונות. שלב 1 של ההתחברות גם חושף אם חשבון קיים (enumeration).

**מה תוקף עושה:** ניחוש שיטתי (brute force) של סיסמאות/קוד SMS, וגילוי חשבונות לפי שם+ת.ז. **תיקון:** להוסיף `express-rate-limit` על נתיבי האימות, ותקרת ניסיונות על ה-OTP.

---

## בינוני (P2)

- **CORS פרוץ בברירת מחדל** — `server/src/index.js:61`, `api/index.js:20`: `origin: env.FRONTEND_URL || '*'` עם `credentials: true`. אם המשתנה ריק — `*` + credentials, צירוף שבור. גם: מקור יחיד לא מתאים לפלטפורמה רב-לקוחות.
- **נקודת הכניסה של Vercel חלשה יותר** — `api/index.js:19`: `helmet({ contentSecurityPolicy: false })` — CSP כבוי לגמרי, בלי הגנות clickjacking. `vercel.json` מחובר בפועל.
- **Regex injection ב-`month`** — `classes.controller.js:117,306`: `{ $regex: \`^${req.query.month}\` }` בלי אימות. מאפשר DoS (`?month=(.*a){40}`). מקום אחר בקוד כבר מאמת `^\d{4}-\d{2}$` — חוסר עקביות.
- **PDF אנשי קשר מחזיר את כל הרשת** — `contacts.controller.js` `generatePDF`: סינון סניף רק אם הלקוח מבקש. בלי פרמטר → כל ילד בכל סניף.
- **תלויות ישנות עם CVE** — `xlsx@0.18.5` (prototype pollution, ReDoS; אין תיקון בקו npm), `multer@1.x` (DoS; תוקן ב-2.x). כדאי `npm audit`.
- **XSS ב-PDF אירועים** — `client/src/components/events/EventDetail.jsx:101`: `innerHTML` משם/טלפון הורה בלי בריחה.

---

## נמוך (P3)

- ת.ז נכתב ללוגים — `server/src/models/Employee.js:478` + `morgan('combined')`.
- הודעות שגיאה גולמיות ללקוח — `err.message` תמיד מוחזר (stack מוסתר ב-production, טוב).
- Path traversal — הגנה חלקית ב-`payslipAudit.controller.js` (לא נצול כרגע).
- העלאת קבצים בלי `fileFilter` — סוג התוכן לא מאומת.
- טופס רישום ציבורי חושף ת.ז — `public.controller.js` `getRegistrationForm`; ממותן ע"י טוקן `uuidv4` (122 ביט), אבל הטוקן בנתיב ה-URL (לוגים, Referer).
- חשיפת/שחרור בחירות באירוע לפי טלפון בלבד — `public.controller.js`.

---

## תפעולי (לא חור בקוד)

**סוד חי במחשב הזה:** `server/.env` מחזיק סיסמת MongoDB Atlas אמיתית בטקסט גלוי (משתמש `totofy10_db_user`, cluster `gan-halomot.pivxzr9`). הקובץ **לא** ב-git (מוגן נכון ב-`.gitignore`), אבל יושב בעותק המקומי. שווה להחליף את הסיסמה כזהירות.

---

## מה שנבדק ונמצא תקין

- הפרדת מפתח בין טוקן הורה לטוקן צוות (`parentAuth.js`) — הצלבה קריפטוגרפית.
- בדיקת שיוך-לקוח בטוקן (`authMiddleware`) — חוסם טוקן של לקוח אחד מול לקוח אחר.
- אימות סוכני Pi ו-sync — השוואה בזמן קבוע (`timingSafeEqual`).
- התחברות מוגנת מ-NoSQL injection — ת.ז מקוצץ לספרות, regex של שם בורח.
- אין סודות ב-git (היסטוריה נקייה), `render.yaml` עם `sync: false` לכל סוד.
- אין command injection, אין SSRF (כל fetch לכתובת קבועה), אין prototype pollution.
- `/me` מסיר `password_hash`. הצפנת סיסמה `bcrypt` cost 10.

---

## המלצת שורש

לאכוף את גבול הסניף **בשרת**: לחתוך כל שאילתה וכל פעולה-לפי-מזהה מול `resolveBranchScope`. לבטל את התנהגות "אין פרמטר = כל הסניפים" לתפקידים שאינם אדמין. להוסיף בדיקת תפקיד/בעלות ל-children, documents, contracts, collections, order, registration, branch. זה סוגר את רוב P0-P1 במכה.
