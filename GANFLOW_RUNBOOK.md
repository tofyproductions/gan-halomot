# GanFlow — מדריך תפעול

איך מקימים לקוח, איך עוזרים לו, ומה אסור לעשות.

נכתב כדי שאפשר יהיה להפעיל את זה בלי מי שבנה אותו.

---

## לפני הכל — שני משתנים שקובעים הכל

| משתנה | מה הוא עושה |
|---|---|
| `PLATFORM_MONGODB_URI` | **מדליק את שכבת הלקוחות.** בלעדיו השרת הוא מערכת של גן אחד, בדיוק כפי שהיה תמיד |
| `MONGODB_URI` | מסד הנתונים של השרת הזה |

### ⚠️ הכלל שאסור לשבור

**אל תגדיר `PLATFORM_MONGODB_URI` על השרת שמשרת את גן החלומות.**

השירות `gan-halomot` ב-Render משרת משפחות אמיתיות. שכבת הלקוחות שייכת לשירות נפרד (`ganflow`). המשתנה הזה הוא ההבדל בין השניים, והוא היחיד.

---

## הקמת לקוח חדש

### 1. מסד נתונים

לקוח מקבל מסד משלו. הכתובת נשמרת אצלו ב-`db_uri`, ושם המסד נגזר מהמזהה (`gf_<slug>`).

**שם המסד שכותבים בכתובת מתעלמים ממנו** — הכתובת קובעת את האשכול בלבד. זו מלכודת שקל ליפול בה.

**כמה לקוחות באשכול אחד:** נמדד — עד 90 לקוחות (7,000 אוספים) הלקוח הראשון לא הרגיש דבר, 1–2 מילישניות לאורך כל הדרך. התקרה המעשית היא סביב 130. הלקוח ה-131 הולך לאשכול אחר — זה שינוי `db_uri` אצלו, לא שכתוב.

### 2. בקונסולה

`/console` ← **+ לקוח חדש**

חובה: שם, כתובת (באנגלית), אימייל מנהל/ת, **שם מלא ותעודת זהות של מנהל/ת**.

**תעודת הזהות אינה פרט מנהלי.** המערכת של הגן מזהה לפי שם מלא **וגם** תעודת זהות. בלעדיה הלקוח מקבל מערכת שאיש לא יכול לפתוח.

### 3. מה שמוצג פעם אחת

אחרי ההקמה מוצגים שלושה פרטים: שם מלא, תעודת זהות, סיסמה זמנית.

**כל השלושה נדרשים לכניסה.** הסיסמה לבדה היא שליש. הם מוצגים פעם אחת ולא נשמרים בצורה קריאה.

מוסרים אותם **בטלפון, לא במייל.** מייל נשאר בתיבה לנצח.

---

## תמיכה — צפייה במערכת של לקוח

בקונסולה, בכרטיס הלקוח: **צפייה במערכת של הלקוח**.

- **צפייה בלבד.** אפשר לראות כל מסך ואי אפשר לשנות דבר
- **פגה אחרי 30 דקות**
- **נרשמת ביומן** עם השם שלך והסיבה, לפני שהגישה נוצרת

**למה אי אפשר לשנות:** לתקן שכר של לקוח בזמן שאתה מחובר בתור המנהלת שלו משאיר רישום שאומר שהמנהלת עשתה את זה. כשצריך לשנות — הלקוח משנה בזמן שאתה מלווה אותו, או שבעלים משנה דרך הקונסולה שם זה נרשם על שמנו.

---

## השהיה, מחיקה, ולקוח שעוזב

### השהיה

הלקוח מאבד גישה מיד. **המידע נשמר במלואו ולא נמחק מעצמו.**

התאריך שמוצג (`purge_after`, חצי שנה) הוא מתי מותר לנו למחוק — לא מתי זה קורה. **שום דבר במערכת לא מוחק נתונים על טיימר**, וזו החלטה מכוונת: השמדה בלתי הפיכה של רשומות ילדים כשאף אחד לא מסתכל היא העבודה האחת שאסור שתרוץ לבד.

### ייצוא — לפני כל מחיקה, ובכל עזיבה

```bash
cd server
PLATFORM_MONGODB_URI="..." node scripts/ganflow-tenant-export.js --slug SLUG --out ~/exports
```

ואז — **תמיד** —

```bash
PLATFORM_MONGODB_URI="..." node scripts/ganflow-tenant-export.js --slug SLUG --out ~/exports --verify
```

הבדיקה משווה את הקבצים **למסד החי**. ייצוא שאי אפשר לוודא את שלמותו הוא לא גיבוי, הוא תקווה.

⚠️ הקבצים מכילים ילדים אמיתיים ואינם מוצפנים.

---

## מה קורה עם העבודות המתוזמנות

על שרת פלטפורמה, כל עבודה רצה **לכל לקוח בנפרד ובטור**. לקוח שנכשל נרשם ומדולג, ולא גורר את השאר. לקוח מושהה לא מקבל עבודות.

**ארבע עבודות כבויות שם בכוונה:** סנכרון גיליונות Google, סיבוס, טופס 101, ודיג׳סט הגיוס.

הן ניגשות לתיבת דואר ולמפתחות **שלנו**. לכל לקוח בנפרד הן היו קוראות את הדואר שלנו בשמו ומתייקות את התוצאה אצלו. יופיעו בלוג ההתנעה — לא צריך לזכור.

**עד שלקוח יוכל לשאת אישורי גישה משלו, הפונקציות האלה לא קיימות עבורו.** אם מוכרים אותן — לומר את זה מראש.

---

## גבולות שנמדדו

| מה | המספר | מה קורה מעבר |
|---|---|---|
| סניפים במסך שכר עובד-עובד | 25 | 413 עם הפניה לסיכום |
| שורות ברשימה בלי עימוד | 5,000 | 413 עם הסבר איך לבקש עמוד |
| לקוחות באשכול | ~130 | הלקוח הבא לאשכול אחר |

**למה יש תקרה על מסך השכר:** ב-2000 סניפים הוא לקח 90 שניות — וזה לא 90 שניות למי שלחץ. Node מריץ הכל בחוט אחד, אז זה 90 שניות **לכולם**, כולל מטפלת שמנסה להחתים כרטיס. נמדד: בקשה אחת כזאת לקחה את מסך הנוכחות מ-211 מילישניות למעל 30 שניות.

מנהל שרוצה לראות את כל הרשת פותח **סיכום לפי יחידות** — שורה למחוז במקום שורה לעובד. נמדד 2–11 מילישניות בכל גודל רשת.

---

## מה עוד לא קיים

- **כתובת נפרדת לכל לקוח** — הקוד יודע לזהות לקוח לפי הכתובת, אבל הדומיין `ganflow.co.il` עדיין לא נרכש
- **חיוב וחשבוניות** — התמחור נשמר אצל הלקוח, אבל שום דבר לא מפיק חשבונית
- **הממשק לא משתמש בעימוד** — השרת מוכן, המסכים עדיין מבקשים הכל. ה-413 מונע את המקרה הגרוע

---

## בדיקות — מה להריץ לפני שנוגעים בייצור

```bash
cd server
npm run platform:isolation   # לקוח לא רואה לקוח אחר
npm run platform:orgscope    # אדם לא רואה מעבר לרמה שלו
npm run demo:test            # הערבול לא משאיר ילדים אמיתיים
npm run platform:scale       # כמה גדול לקוח יכול להיות
npm run platform:cluster     # כמה לקוחות באשכול
```

שלוש הראשונות חייבות לעבור. השתיים האחרונות הן מדידה, לא בדיקה — הן מדפיסות מספרים ולא נכשלות.
