{
  "firstLoadGzipKB": 249,
  "recorded": "2026-09-10",
  "was": "1168 KB gzip / 3.99 MB raw, in one chunk, before routes and vendors were split",
  "note": "What index.html pulls in before first paint: the entry chunk plus every modulepreload. Lazily-imported screens are deliberately not counted — that is the point. Enforced by server/scripts/bundle-budget.test.js, which must run after a build.",
  "history": [
    "250 — routes and vendors split",
    "252 — +2 KB for the breadcrumb, the 404 page and the screen-name map. Raised deliberately: the three heavy-library checks still pass, so this is new behaviour rather than a library creeping back onto the critical path. A budget raised without a reason written here is not a budget.",
    "254 — +2 KB for the error boundary around the lazy screens. A failed chunk on a gan's wifi was a white page that a reload could not fix, because React caches the rejected promise.",
    {
      "date": "2026-09-10",
      "from": 254,
      "to": 255,
      "why": "Rail screen-search + recents (InputBase, 3 icons). Approved deliberately; no library involved."
    },
    {
      "date": "2026-09-10",
      "from": 255,
      "to": 258,
      "why": "Merge of origin/main: UpdateBanner (mounted in main.jsx, must be eager) and NotificationsPanel (the first thing on the dashboard, so not a lazy candidate). No library — the three heavy vendors are still lazy, asserted below."
    },
    {
      "date": "2026-09-10",
      "from": 258,
      "to": 259,
      "why": "AcademicYearProvider + the year picker in the rail. The rail is on the critical path by definition; six per-screen pickers came off the lazy chunks in exchange."
    },
    {
      "date": "2026-09-10",
      "from": 259,
      "to": 249,
      "why": "Two complete interfaces now ship (classic + new), and BOTH shells became lazy — a person uses exactly one, so neither belongs in the first download. Net 259 -> 249 despite the addition."
    }
  ]
}
